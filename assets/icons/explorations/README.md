# explorations

Sheets made to decide something, kept as the record of what was looked at.

- `launch.png`, from `launch.mjs` — the current `launch` beside three redraws, each a solid mass as spec §6 requires, at 16 to 64 px as shipped and at 128 px under the haze. Izzy picked B on 2026-09-06; it ships as `launchNotch`. The current panel does not read as "open" at UI sizes; the candidates put clear air between the arrow and the panel in three ways: a knocked-out corner (B), two separate masses (D), a channel cut around the arrow (E).
